# 平台(门户)登录密码（全部账号的明文密码统一存放于此文件）
# 格式：每行一条 “用户名:密码”（密码非空且不超过128字符，不能含冒号/首尾空格；# 开头为注释）
# 门户里发生的建号/改密/删除会自动同步本文件；OS root 直接编辑本文件也会在 ~20s 内自动生效
# 迁移机器时可直接把旧机整份文件原样复制过来，以保证账号密码一致；或按需改写后再同步。
# 示例：
# alice:ChangeMe_2026
# root:RootPwd_2026
